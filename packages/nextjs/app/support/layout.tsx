export const metadata = {
  title: "Support",
  description: "Get help with the Pipette Base Sepolia faucet. Contact support.",
};

export default function SupportLayout({ children }: { children: React.ReactNode }) {
  return children;
}

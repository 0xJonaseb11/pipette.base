{
  "installCommand": "corepack enable && corepack prepare yarn@3.2.3 --activate && yarn --version && yarn install --immutable",
  "crons": [
    {
      "path": "/api/cron/refill",
      "schedule": "0 5 * * *"
    }
  ]
}

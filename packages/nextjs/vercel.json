{
  "installCommand": "cd ../.. && corepack enable && corepack prepare yarn@3.2.3 --activate && corepack yarn --version && corepack yarn install --immutable",
  "buildCommand": "corepack yarn build",
  "crons": [
    {
      "path": "/api/cron/refill",
      "schedule": "0 5 * * *"
    }
  ]
}
